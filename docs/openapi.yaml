openapi: 3.0.3
info:
  title: 守望者行情数据服务 API（Warden Stock Data Service）
  description: >
    行情数据中台。两套 BasePath：
    管理 API `/admin`（管理员 JWT 鉴权，可读写）；
    开放 API `/open/v1`（secretId/secretKey HMAC 签名鉴权，**纯只读**）。
    所有金额/价格/比率/指标值为 decimal 字符串（见 components.schemas.Decimal）。
  version: 1.0.0
servers:
  - url: http://localhost:8080
    description: 本地开发

tags:
  - name: AdminAuth
    description: 管理员认证
  - name: Credentials
    description: 接入凭证管理（M5，仅管理员）
  - name: DataSources
    description: 数据源与更新作业管理（M1/M2，仅管理员）
  - name: AdminMarket
    description: 管理后台行情只读代理（M6，管理员 JWT，复用开放 API service 层）
  - name: OpenMarket
    description: 开放行情只读接口（M4，HMAC 凭证）
  - name: OpenIndicator
    description: 开放技术指标只读接口（M3/M4，HMAC 凭证）
  - name: OpenMeta
    description: 开放元数据只读接口（M4，HMAC 凭证）

paths:
  # ============== 管理 API ==============
  /admin/auth/login:
    post:
      tags: [AdminAuth]
      summary: 管理员登录
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, password]
              properties:
                username: { type: string, example: admin }
                password: { type: string, example: "admin123" }
      responses:
        "200":
          description: 登录成功
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties:
                      data:
                        type: object
                        properties:
                          token: { type: string }
        "400": { $ref: "#/components/responses/BadRequest" }

  /admin/auth/me:
    get:
      tags: [AdminAuth]
      summary: 当前管理员信息
      security: [{ AdminJWT: [] }]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/Admin" } }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /admin/credentials:
    get:
      tags: [Credentials]
      summary: 凭证列表
      security: [{ AdminJWT: [] }]
      parameters:
        - { in: query, name: page, schema: { type: integer, default: 1 } }
        - { in: query, name: size, schema: { type: integer, default: 20 } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties:
                      data:
                        type: object
                        properties:
                          list: { type: array, items: { $ref: "#/components/schemas/Credential" } }
                          total: { type: integer }
                          page: { type: integer }
                          size: { type: integer }
    post:
      tags: [Credentials]
      summary: 创建凭证（一次性返回 secretKey 明文）
      security: [{ AdminJWT: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateCredentialReq" }
      responses:
        "200":
          description: 创建成功，secretKey 仅此一次返回
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/CredentialSecret" } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /admin/credentials/{id}:
    parameters: [{ in: path, name: id, required: true, schema: { type: integer } }]
    get:
      tags: [Credentials]
      summary: 凭证详情 + 调用审计
      security: [{ AdminJWT: [] }]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/CredentialDetail" } }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [Credentials]
      summary: 修改配额 / 启停
      security: [{ AdminJWT: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                rate_limit: { type: integer }
                daily_quota: { type: integer }
                status: { type: integer, enum: [0, 1] }
                expire_at: { type: string, format: date-time, nullable: true }
      responses:
        "200": { $ref: "#/components/responses/OK" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [Credentials]
      summary: 吊销凭证
      security: [{ AdminJWT: [] }]
      responses:
        "200": { $ref: "#/components/responses/OK" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/credentials/{id}/rotate:
    post:
      tags: [Credentials]
      summary: 轮换 secretKey（一次性返回新明文）
      security: [{ AdminJWT: [] }]
      parameters: [{ in: path, name: id, required: true, schema: { type: integer } }]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/CredentialSecret" } }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/datasources:
    get:
      tags: [DataSources]
      summary: 数据源列表 + 健康状态
      security: [{ AdminJWT: [] }]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { type: array, items: { $ref: "#/components/schemas/DataSource" } } }

  /admin/datasources/{id}:
    put:
      tags: [DataSources]
      summary: 配置数据源
      security: [{ AdminJWT: [] }]
      parameters: [{ in: path, name: id, required: true, schema: { type: integer } }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled: { type: boolean }
                priority: { type: integer }
                config: { type: object, additionalProperties: true }
      responses:
        "200": { $ref: "#/components/responses/OK" }

  /admin/datasources/{id}/healthcheck:
    post:
      tags: [DataSources]
      summary: 触发连通性探测
      security: [{ AdminJWT: [] }]
      parameters: [{ in: path, name: id, required: true, schema: { type: integer } }]
      responses:
        "200": { $ref: "#/components/responses/OK" }

  /admin/jobs:
    get:
      tags: [DataSources]
      summary: 更新作业列表
      security: [{ AdminJWT: [] }]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { type: array, items: { $ref: "#/components/schemas/UpdateJob" } } }

  /admin/jobs/{id}:
    put:
      tags: [DataSources]
      summary: 配置作业（名称 / 市场 / cron / 分批 / 并发 / 启停）
      description: |
        修改作业配置。job_type 与创建/更新时间字段不可修改，其余字段均可改（按需传入）。
        cron_expr 会按调度器使用的 6 段（含秒）格式做服务端校验，不合法返回 400。
      security: [{ AdminJWT: [] }]
      parameters: [{ in: path, name: id, required: true, schema: { type: integer } }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string, example: "A股每日增量更新" }
                market: { type: string, example: CN }
                cron_expr: { type: string, example: "0 0 17 * * *", description: "6 段含秒格式，服务端校验" }
                batch_size: { type: integer, example: 20, description: "必须 > 0" }
                concurrency: { type: integer, example: 10, description: "必须 > 0" }
                enabled: { type: boolean }
      responses:
        "200": { $ref: "#/components/responses/OK" }
        "400": { description: "参数错误，如 cron_expr 格式不合法 / batch_size / concurrency 非正" }

  /admin/jobs/{id}/run:
    post:
      tags: [DataSources]
      summary: 手动触发一次更新作业
      description: 作业 `enabled=false` 时拒绝触发，返回 400。
      security: [{ AdminJWT: [] }]
      parameters: [{ in: path, name: id, required: true, schema: { type: integer } }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type: { type: string, enum: [full, incremental, snapshot, indicator, securities], default: incremental, description: "securities：仅同步证券列表（轻量，不拉 K 线）；incremental：按 watermark 仅更新落后标的" }
                market: { type: string, default: CN }
                codes: { type: array, items: { type: string }, description: "为空表示全市场；增量类型留空时按 watermark 自动筛待更新" }
      responses:
        "200":
          description: |
            已派发。若同类型作业已在运行，则本次进入排队（status=waiting），
            待前序同类型作业完成后自动执行；waiting 作业可通过取消接口撤销。
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties:
                      data:
                        type: object
                        properties:
                          runId: { type: integer }
                          status: { type: string, enum: [running, waiting] }
        "400": { description: 作业已停用，无法手动触发 }

  /admin/jobs/runs:
    get:
      tags: [DataSources]
      summary: 作业执行记录（分页）
      security: [{ AdminJWT: [] }]
      parameters:
        - { in: query, name: page, schema: { type: integer, default: 1 } }
        - { in: query, name: size, schema: { type: integer, default: 20 } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties:
                      data:
                        type: object
                        properties:
                          list: { type: array, items: { $ref: "#/components/schemas/JobRun" } }
                          total: { type: integer }

  /admin/jobs/runs/{runId}:
    get:
      tags: [DataSources]
      summary: 单次执行详情 / 进度
      security: [{ AdminJWT: [] }]
      parameters: [{ in: path, name: runId, required: true, schema: { type: integer } }]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/JobRun" } }

  /admin/jobs/runs/{runId}/cancel:
    post:
      tags: [DataSources]
      summary: 取消运行中作业
      security: [{ AdminJWT: [] }]
      parameters: [{ in: path, name: runId, required: true, schema: { type: integer } }]
      responses:
        "200": { $ref: "#/components/responses/OK" }

  /admin/freshness:
    get:
      tags: [DataSources]
      summary: 数据新鲜度
      security: [{ AdminJWT: [] }]
      parameters: [{ in: query, name: market, schema: { type: string, default: CN } }]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/Freshness" } }

  /admin/market/indices:
    get:
      tags: [AdminMarket]
      summary: 大盘指数（管理后台只读）
      security: [{ AdminJWT: [] }]
      parameters: [{ in: query, name: market, schema: { type: string, default: CN } }]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { type: array, items: { $ref: "#/components/schemas/IndexQuote" } } }

  /admin/market/search:
    get:
      tags: [AdminMarket]
      summary: 股票搜索（管理后台只读）
      description: 按代码/名称模糊匹配，优先查本地 securities 表（trgm 索引）；库内无数据时回源行情提供方。
      security: [{ AdminJWT: [] }]
      parameters:
        - { in: query, name: kw, required: true, schema: { type: string } }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { type: array, items: { $ref: "#/components/schemas/StockBrief" } } }

  /admin/market/stocks/{code}:
    get:
      tags: [AdminMarket]
      summary: 单只个股快照（管理后台只读）
      security: [{ AdminJWT: [] }]
      parameters:
        - { in: path, name: code, required: true, schema: { type: string } }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/StockQuote" } }

  /admin/market/stocks/{code}/kline:
    get:
      tags: [AdminMarket]
      summary: 个股 K 线（管理后台只读）
      security: [{ AdminJWT: [] }]
      parameters:
        - { in: path, name: code, required: true, schema: { type: string } }
        - { in: query, name: period, schema: { type: string, enum: [day, week, month], default: day } }
        - { in: query, name: adjust, schema: { type: string, enum: ["", qfq, hfq], default: qfq } }
        - { in: query, name: limit, schema: { type: integer, default: 120 } }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { type: array, items: { $ref: "#/components/schemas/Kline" } } }

  /admin/market/stocks/{code}/intraday:
    get:
      tags: [AdminMarket]
      summary: 个股当日分时走势（管理后台只读）
      security: [{ AdminJWT: [] }]
      parameters:
        - { in: path, name: code, required: true, schema: { type: string } }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/Intraday" } }

  /admin/market/stocks/{code}/indicators:
    get:
      tags: [AdminMarket]
      summary: 个股技术指标（管理后台只读）
      security: [{ AdminJWT: [] }]
      parameters:
        - { in: path, name: code, required: true, schema: { type: string } }
        - { in: query, name: types, schema: { type: string, default: "ma5,ma10,ma20,ma30,ma60" }, description: "逗号分隔指标类型，取值见 indicator_catalog" }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/IndicatorResult" } }

  # ============== 开放 API（HMAC 凭证，只读） ==============
  /open/v1/indices:
    get:
      tags: [OpenMarket]
      summary: 大盘指数
      security: [{ HmacSignature: [] }]
      parameters: [{ in: query, name: market, schema: { type: string, default: CN } }]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { type: array, items: { $ref: "#/components/schemas/IndexQuote" } } }
        "401": { $ref: "#/components/responses/CredentialError" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /open/v1/quotes:
    get:
      tags: [OpenMarket]
      summary: 批量个股快照
      security: [{ HmacSignature: [] }]
      parameters:
        - { in: query, name: codes, required: true, schema: { type: string }, description: "逗号分隔，如 600000,000001" }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { type: array, items: { $ref: "#/components/schemas/StockQuote" } } }
        "401": { $ref: "#/components/responses/CredentialError" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /open/v1/stocks/{code}:
    get:
      tags: [OpenMarket]
      summary: 单只个股快照
      security: [{ HmacSignature: [] }]
      parameters:
        - { in: path, name: code, required: true, schema: { type: string } }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/StockQuote" } }
        "404": { $ref: "#/components/responses/NotFound" }

  /open/v1/stocks/{code}/kline:
    get:
      tags: [OpenMarket]
      summary: 个股 K 线（支持 limit 或 from/to 区间，回测取历史区间用）
      security: [{ HmacSignature: [] }]
      parameters:
        - { in: path, name: code, required: true, schema: { type: string } }
        - { in: query, name: period, schema: { type: string, enum: [day, week, month, 1m, 5m, 15m, 30m, 60m], default: day } }
        - { in: query, name: adjust, schema: { type: string, enum: ["", qfq, hfq], default: qfq } }
        - { in: query, name: limit, schema: { type: integer, default: 120 }, description: "最近 N 根；与 from/to 互斥，区间优先" }
        - { in: query, name: from, schema: { type: string, format: date }, description: "起始交易日（含），回测区间用" }
        - { in: query, name: to, schema: { type: string, format: date }, description: "结束交易日（含），回测区间用" }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { type: array, items: { $ref: "#/components/schemas/Kline" } } }

  /open/v1/stocks/{code}/intraday:
    get:
      tags: [OpenMarket]
      summary: 个股分时走势（实时透传；非交易日自动回退最近交易日，trade_date 标注实际日期）
      security: [{ HmacSignature: [] }]
      parameters:
        - { in: path, name: code, required: true, schema: { type: string } }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/Intraday" } }

  /open/v1/stocks/{code}/indicators:
    get:
      tags: [OpenIndicator]
      summary: 单只个股技术指标（实时计算，缺失回退）
      description: >
        逗号分隔指标类型。可用类型见 /open/v1/meta 的 indicator_catalog，含：
        均线 ma5/ma10/ma20/ma30/ma60；乖离 bias5/bias10/bias20；ma_align；
        振幅 amplitude/amplitude_streak；动量 pct_change/pct_change5/pct_change20/pct_change60；
        量比 vol_ratio；MACD macd_dif/macd_dea/macd_bar(12,26,9)；
        KDJ kdj_k/kdj_d/kdj_j(9,3,3)；RSI rsi6/rsi12/rsi24；
        BOLL boll_mid/boll_upper/boll_lower(20,2)；ATR atr14/atr20。
      security: [{ HmacSignature: [] }]
      parameters:
        - { in: path, name: code, required: true, schema: { type: string } }
        - { in: query, name: types, schema: { type: string, default: "ma5,ma10,ma20,ma30,ma60" }, description: "逗号分隔指标类型，取值见 indicator_catalog" }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/IndicatorResult" } }

  /open/v1/indicators:
    get:
      tags: [OpenIndicator]
      summary: 批量个股技术指标（读快照，量化因子 / 回测用）
      description: >
        读 stock_indicator_snapshots 历史指标快照。指定 trade_date 即取该交易日的
        point-in-time 指标（无未来函数），可用于按日回放的量化回测信号生成。
        盘后全市场扫描默认落库的指标集合：ma5/10/20/30/60、macd_dif/dea/bar、
        kdj_k/d/j、rsi6/12/24、boll_mid/upper/lower、atr14/atr20、pct_change20/60；
        其余指标（如 bias、vol_ratio）可经单只实时接口按需计算。
      security: [{ HmacSignature: [] }]
      parameters:
        - { in: query, name: codes, required: true, schema: { type: string }, description: "逗号分隔" }
        - { in: query, name: types, schema: { type: string, default: "ma5,ma10,ma20,ma30,ma60" }, description: "逗号分隔指标类型，取值见 indicator_catalog" }
        - { in: query, name: trade_date, schema: { type: string, format: date }, description: "指定历史交易日(point-in-time)；为空取最新交易日" }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { type: array, items: { $ref: "#/components/schemas/IndicatorResult" } } }

  /open/v1/search:
    get:
      tags: [OpenMarket]
      summary: 股票搜索（按代码 / 名称）
      security: [{ HmacSignature: [] }]
      parameters:
        - { in: query, name: kw, required: true, schema: { type: string } }
        - { in: query, name: market, schema: { type: string, default: CN } }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { type: array, items: { $ref: "#/components/schemas/StockBrief" } } }

  /open/v1/securities:
    get:
      tags: [OpenMeta]
      summary: 证券列表
      security: [{ HmacSignature: [] }]
      parameters: [{ in: query, name: market, schema: { type: string, default: CN } }]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { type: array, items: { $ref: "#/components/schemas/StockBrief" } } }

  /open/v1/meta:
    get:
      tags: [OpenMeta]
      summary: 元数据（市场列表 / 指标目录 / 数据新鲜度）
      security: [{ HmacSignature: [] }]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiResponse"
                  - properties: { data: { $ref: "#/components/schemas/Meta" } }

components:
  securitySchemes:
    AdminJWT:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: "管理 API 鉴权，Header `Authorization: Bearer <jwt>`"
    HmacSignature:
      type: apiKey
      in: header
      name: X-Signature
      description: >
        开放 API 鉴权。需同时携带 Header：X-Secret-Id、X-Timestamp、X-Nonce、X-Signature。
        签名串 = METHOD\nPATH\nCanonicalQuery\nX-Secret-Id\nX-Timestamp\nX-Nonce\nSHA256Hex(body)，
        Signature = Base64(HMAC_SHA256(secretKey, 签名串))。时间戳偏差 ±300s，nonce 防重放。

  responses:
    OK:
      description: 操作成功
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiResponse" }
    BadRequest:
      description: 参数错误
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiResponse" }
    Unauthorized:
      description: 管理员未登录 / token 非法（40001）
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiResponse" }
    NotFound:
      description: 资源不存在
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiResponse" }
    CredentialError:
      description: 凭证鉴权失败（41001 缺签名 / 41002 凭证无效 / 41003 签名错 / 41004 重放过期 / 41005 scope 不足）
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiResponse" }
    RateLimited:
      description: 触发限流或超配额（42001 / 42002）
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiResponse" }

  schemas:
    Decimal:
      type: string
      description: 高精度数值（decimal），序列化为带引号字符串，如 "10.5000"。前端需转 number 再计算。
      example: "10.5000"

    ApiResponse:
      type: object
      properties:
        code: { type: integer, example: 0 }
        message: { type: string, example: ok }
        data: {}

    Admin:
      type: object
      properties:
        id: { type: integer }
        username: { type: string }
        role: { type: string, example: admin }

    CreateCredentialReq:
      type: object
      required: [consumer_name]
      properties:
        consumer_name: { type: string, example: "warden-stock-trading" }
        rate_limit: { type: integer, default: 20, description: "QPS" }
        daily_quota: { type: integer, default: 100000 }
        expire_at: { type: string, format: date-time, nullable: true }

    Credential:
      type: object
      properties:
        id: { type: integer }
        secret_id: { type: string, example: "AKIDxxxxxxxxxxxxxxxxxxxxxxxxxxxx" }
        consumer_name: { type: string }
        scope: { type: string, example: read }
        rate_limit: { type: integer }
        daily_quota: { type: integer }
        status: { type: integer, enum: [0, 1] }
        expire_at: { type: string, format: date-time, nullable: true }
        created_at: { type: string, format: date-time }

    CredentialSecret:
      type: object
      description: 仅创建 / 轮换时返回一次，secret_key 之后不可再获取
      properties:
        secret_id: { type: string }
        secret_key: { type: string, description: "明文，仅此一次返回" }

    CredentialDetail:
      allOf:
        - $ref: "#/components/schemas/Credential"
        - type: object
          properties:
            access_stats:
              type: array
              items:
                type: object
                properties:
                  stat_date: { type: string, format: date }
                  call_count: { type: integer }
                  error_count: { type: integer }
                  last_access_at: { type: string, format: date-time }

    DataSource:
      type: object
      properties:
        id: { type: integer }
        source: { type: string, example: gotdx }
        market: { type: string, example: CN }
        name: { type: string }
        enabled: { type: boolean }
        priority: { type: integer }
        health: { type: string, enum: [ok, degraded, down, unknown] }

    UpdateJob:
      type: object
      properties:
        id: { type: integer }
        name: { type: string }
        job_type: { type: string, enum: [full, incremental, snapshot, indicator, securities] }
        market: { type: string, example: CN }
        cron_expr: { type: string, example: "0 0 17 * * *" }
        batch_size: { type: integer, example: 20 }
        concurrency: { type: integer, example: 10 }
        enabled: { type: boolean }

    JobRun:
      type: object
      properties:
        id: { type: integer }
        job_id: { type: integer }
        job_type: { type: string, enum: [full, incremental, snapshot, indicator, securities] }
        market: { type: string, example: CN }
        status: { type: string, enum: [waiting, running, done, failed, canceled], description: "waiting：同类型已有运行中作业，排队等待；可被取消" }
        total: { type: integer }
        processed: { type: integer }
        succeeded: { type: integer }
        failed: { type: integer }
        started_at: { type: string, format: date-time }
        finished_at: { type: string, format: date-time, nullable: true }
        error_msg: { type: string }

    Freshness:
      type: object
      properties:
        market: { type: string, example: CN }
        latest_trade_date: { type: string, format: date }
        kline_updated_to: { type: string, format: date }
        last_scan_at: { type: string, format: date-time, nullable: true }
        securities_count: { type: integer }
        kline_stock_count: { type: integer, description: "库中已落库行情数据的股票数量（已从数据源拉取覆盖度）" }

    IndexQuote:
      type: object
      properties:
        market: { type: string, example: CN }
        index_code: { type: string, example: "000001" }
        index_name: { type: string, example: "上证指数" }
        price: { $ref: "#/components/schemas/Decimal" }
        change_amount: { $ref: "#/components/schemas/Decimal" }
        change_percent: { $ref: "#/components/schemas/Decimal" }
        volume: { $ref: "#/components/schemas/Decimal" }
        amount: { $ref: "#/components/schemas/Decimal" }
        trade_date: { type: string, format: date-time }

    StockQuote:
      type: object
      properties:
        market: { type: string, example: CN }
        stock_code: { type: string, example: "600000" }
        stock_name: { type: string }
        price: { $ref: "#/components/schemas/Decimal" }
        open: { $ref: "#/components/schemas/Decimal" }
        high: { $ref: "#/components/schemas/Decimal" }
        low: { $ref: "#/components/schemas/Decimal" }
        prev_close: { $ref: "#/components/schemas/Decimal" }
        change_percent: { $ref: "#/components/schemas/Decimal" }
        volume: { $ref: "#/components/schemas/Decimal" }
        amount: { $ref: "#/components/schemas/Decimal" }
        turnover_rate: { $ref: "#/components/schemas/Decimal" }
        trade_date: { type: string, format: date-time }
        stale: { type: boolean, description: "true 表示数据源异常时返回的降级快照" }

    Kline:
      type: object
      properties:
        date: { type: string, example: "2026-06-05" }
        open: { $ref: "#/components/schemas/Decimal" }
        high: { $ref: "#/components/schemas/Decimal" }
        low: { $ref: "#/components/schemas/Decimal" }
        close: { $ref: "#/components/schemas/Decimal" }
        volume: { $ref: "#/components/schemas/Decimal" }
        amount: { $ref: "#/components/schemas/Decimal" }

    IntradayPoint:
      type: object
      description: 单个交易分钟的分时数据点
      properties:
        time: { type: string, format: date-time, example: "2026-06-05T09:30:00+08:00" }
        price: { $ref: "#/components/schemas/Decimal" }
        avg_price: { $ref: "#/components/schemas/Decimal" }
        volume: { $ref: "#/components/schemas/Decimal" }

    Intraday:
      type: object
      description: 当日分时走势（实时透传，不落库）
      properties:
        market: { type: string, example: CN }
        stock_code: { type: string }
        stock_name: { type: string }
        trade_date: { type: string, example: "2026-06-05" }
        pre_close: { $ref: "#/components/schemas/Decimal" }
        points:
          type: array
          items: { $ref: "#/components/schemas/IntradayPoint" }

    StockBrief:
      type: object
      properties:
        stock_code: { type: string }
        stock_name: { type: string }
        market: { type: string, example: CN }
        board: { type: string, example: "主板" }

    IndicatorResult:
      type: object
      properties:
        stock_code: { type: string }
        trade_date: { type: string, format: date }
        values:
          type: object
          additionalProperties: { $ref: "#/components/schemas/Decimal" }
          example: { ma5: "12.3400", ma20: "11.8000", ma60: "10.9000", macd_bar: "0.1500", kdj_k: "72.3000", rsi6: "65.0000", boll_upper: "13.2000", atr14: "0.4500" }

    Meta:
      type: object
      properties:
        markets:
          type: array
          items: { type: string }
          example: [CN]
        indicator_catalog:
          type: array
          items:
            type: object
            properties:
              type: { type: string, example: ma }
              name: { type: string, example: "移动均线 MA" }
              value_type: { type: string, enum: [number, bool] }
              unit: { type: string }
              params: { type: array, items: { type: object } }
        freshness: { $ref: "#/components/schemas/Freshness" }
