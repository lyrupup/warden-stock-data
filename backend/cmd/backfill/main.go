// Package main provides a CLI placeholder for historical K-line backfill (M2).
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Println("warden backfill: use admin POST /admin/jobs/:id/run for incremental jobs in V1 foundation")
	os.Exit(0)
}
